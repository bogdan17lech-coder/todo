# Test profile for ToDo – always H2 in-memory
spring:
  datasource:
    url: jdbc:h2:mem:todo_test;MODE=MySQL;DB_CLOSE_DELAY=0
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    open-in-view: false
